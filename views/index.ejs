<h2>Blogs</h2>
<div>
  <% blogs.forEach(blog => { %>
  <a href="/blogs/<%= blog.id %>">
    <div><%= blog.title %></div>
    <img height="150" width="150" src="<%= blog.coverImagePath %>">
  </a>
  <% }) %>
</div>